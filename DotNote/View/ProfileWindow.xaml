<Window x:Class="DotNote.View.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:DotNote.ViewModel.Login"
        xmlns:local="clr-namespace:DotNote.View"
        mc:Ignorable="d"
        Title="Profile" Height="300" Width="300">

    <Window.Resources>
        <ResourceDictionary>
            <vm:ProfileVM x:Key="ProfileVM"/>
        </ResourceDictionary>
    </Window.Resources>

    <StackPanel Margin="10">
        <TextBlock Text="Email: "/>
        <TextBox Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Focusable="False"/>

        <TextBlock Text="First Name: "/>
        <TextBox Text="{Binding FirstName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Last Name: "/>
        <TextBox Text="{Binding LastName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- TODO - implement buttons to edit stuff

        <StackPanel Margin="0 10 0 0">
            <Button Content="Update Password" />
            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                    Content="Save"
                    Command="{Binding SaveProfileCommand}" />

                <Button Grid.Column="1"
                    Content="Cancel"
                    Click="Cancel_Click" />
            </Grid>
        </StackPanel>
        
        -->
    </StackPanel>
</Window>
